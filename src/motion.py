"""Motion module: 2.5D parallax + procedural-FX render engine.

Drives Tiers A (static + FX) and B (parallax) locally via moviepy/ffmpeg — the
free-motion workhorse that keeps per-video cost down. Renders each shot to a
clip for timeline.py to assemble.

TODO: implement.
"""
